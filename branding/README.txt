Drop the artist logo here as logo.png (PNG with transparency, roughly 300px wide
or larger). It shows in the top bar of the site next to SNAP-STUDIO! — it is
never drawn into the saved photos. Nothing else to configure; if the file is
missing the space simply stays hidden.
